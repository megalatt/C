#include <stdio.h>
int main() {
	int n, result;
	printf("Enter an integer : ");
	scanf("%d", &n);
	result =(n <<2) +(n<<1) +n- 0 ; // Fill in the missing code
	printf("The result : %d\n", result);
	return 0;
}
