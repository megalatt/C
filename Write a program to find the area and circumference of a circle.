#include<stdio.h>
#define PI 3.14
int main()
{
	float radius,area,circumference;
	printf("radius : ");
	scanf("%f",&radius);
	area = PI * radius * radius;
		circumference = 2 * PI * radius;
	printf("area = %f",area);
	printf("\ncircumference = %f\n", circumference);
	return 0;
	
}
