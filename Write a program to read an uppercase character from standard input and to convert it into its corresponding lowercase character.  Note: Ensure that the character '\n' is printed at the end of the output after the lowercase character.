#include<stdio.h>
int main()
{
    char upperChar, lowerChar;
    int ascii;
    printf("uppercase character : ");
    scanf("%c",&upperChar);
    ascii = upperChar;
    lowerChar = ascii+32;
    printf("lowercase : %c\n",lowerChar);
    return 0;
}
