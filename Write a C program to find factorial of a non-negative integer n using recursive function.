#include <stdio.h>
int Factorial(int N){
	  if (N == 0)  
    return 1;  
  else  
    return(N * Factorial(N-1)); 
}
int main() {
	
	int number;  
  long fact;  
    scanf("%d", &number);
   
  fact = Factorial(number);  
  printf("%ld\n",fact);  
  return 0;  
}
